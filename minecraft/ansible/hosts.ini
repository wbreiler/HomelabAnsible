# hosts.ini — static inventory for provision.yml
#
# Play 1 runs on localhost (against the Proxmox API).
# Play 2's `newly_provisioned` group is built dynamically via add_host in Play 1,
# so no static entries are needed for the Minecraft LXCs here.

[local]
localhost ansible_connection=local
