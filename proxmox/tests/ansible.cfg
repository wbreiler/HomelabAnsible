[defaults]
roles_path = ../roles
# Test playbooks target localhost through a local connection; SSH is not used.
host_key_checking = False
stdout_callback = ansible.posix.debug
callback_result_format = yaml
